# SecurePipeline Hub — React Dashboard

```bash
# Terminal 1 — Start the API (from project root)
python api/app.py

# Terminal 2 — Start the dashboard
cd dashboard
npm start
```

Dashboard runs at: `http://localhost:3000`
API runs at: `http://localhost:5000`
